import { makeStyles } from '@material-ui/core/styles';

const classes = makeStyles(() => ({
  primary: {
    background: 'red',
    color: 'white',
  },

  secondary: {
    background: 'purple',
    color: 'white',
  },
}));

export default classes;
